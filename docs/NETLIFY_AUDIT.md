# netlify_audit
- Projeto encontrado via plugin Netlify: `prompt-app-dan` (`site_id` `2628e92e-47d5-40bb-abaa-be25612b2d56`), deploy atual `ready`.
- `netlify.toml` presente com:
  - build `pnpm install && pnpm run build`
  - headers de seguranca e CSP
  - redirect HTTP->HTTPS
  - SPA fallback para `/index.html`
- Auditoria web (squirrel, surface/full):
  - Score geral: **97 (A)**
  - Falha: `crawl/sitemap-valid` (sitemaps `page-sitemap.xml` e `news-sitemap.xml` em formato nao esperado)
  - Warnings: duplicate title/description, thin content, critical request chain CSS.
- Impacto:
  - Nao bloqueia funcionamento app/sync.
  - Afeta SEO/crawl consistency.

interface:
  display_name: "PROMPT APP"
  short_description: "Repo-specific patterns and workflows for PROMPT-APP"
  default_prompt: "Use the PROMPT-APP repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true